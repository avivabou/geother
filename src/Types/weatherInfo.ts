type DayForecast = {
  title: string;
  weatherStatus: string;
  iconCode: number;
  temperature: string;
  key: string;
};

// eslint-disable-next-line import/prefer-default-export
export { DayForecast };
